<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>xr_link_test</name>
  <version>0.1.0</version>
  <description>
    Link verification between a Samsung Galaxy XR headset and ROS 2 Jazzy on a
    Qualcomm board (RubikPi 3 now, IQ-9075 later). Prints every message that
    reaches the ROS graph, with per-topic rate and jitter, so the headset link
    can be proven before any robot hardware is involved.
  </description>
  <maintainer email="swapnilvnit2018@gmail.com">Swapnil</maintainer>
  <license>Apache-2.0</license>

  <exec_depend>rclpy</exec_depend>
  <exec_depend>rosidl_runtime_py</exec_depend>

  <!-- Message packages the formatters render. All are part of ros-jazzy-desktop
       and ros-jazzy-ros-base, but declaring them makes rosdep do the right
       thing on a minimal install. -->
  <exec_depend>std_msgs</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>nav_msgs</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
